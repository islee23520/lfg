# omo-codex → Grok parity (lfg owner)

Status column updated during `.omo/plans/grok-native-omo-hooks.md` execution (T11 checkbox).

`lfg` is the npm entry for the **omo / lazycodex GrokBuild port** and **Grok Build plugin payload** (core **codex adapter** feature + **opencode** lineage from https://github.com/code-yeongyu/oh-my-openagent). The package/JSON contract keeps `lfgIsPlugin: false` because the npm package object is not reported as a Grok plugin object; `setup --run` installs the lfg-owned Grok plugin payload under `~/.grok/plugins/lfg`.

`setup --run` installs **native Grok hooks** (first-party OMO/lfg hook payloads as native Grok lifecycle events) and Grok agent/prompt surfaces for the default Hephaestus discipline, ultrawork, lfg-owned Sisyphus/Atlas planning and research surfaces, and role agents. It uses **bridge fallback** only for legacy/imported hooks. **Scoped Grok-first OMO parity** is achieved via native hooks, installed agent/prompt payloads, and the `~/.grok` payload under `~/.grok/plugins/lfg` (with legacy `~/.grok/installed-plugins/lfg` as migration/fallback, and `lfg-install.json` plus `lfg-component-inventory.json` for verification). Full native OMO agent behavioral parity is not claimed when install-only receipts report `nativeAgentsStatus: "missing"`; full `setup --run` must be used to verify the agent ledger surface.

Issue #36 separates already-shipped core install parity from full OMO component parity.

## Current Parity Score

lfg reports **two scores**. Do not collapse them: **adapter-scope 100 is not full-OMO 100**.

| Score | Name | Value | Question it answers |
|-------|------|------:|---------------------|
| **A** | **GrokBuild adapter scope** | **100/100** | Did lfg finish the Grok/lfg contract under `~/.grok` (install, cores, hooks/MCP/skills in scope), treating host-bound / senpi / codex_app planes as **out of contract**? |
| **B** | **Full OMO host surface** | **89/100** | If every upstream OMO *component row* is graded as if Grok must own it end-to-end (including codex_app / missing host / auto-reinject), how complete is that? |

**Canonical product claim for “lfg is done for its scope”:** Score **A = 100/100**.  
**Canonical honesty claim for “full OMO on Grok like Codex/OpenCode”:** Score **B = 89/100** (inventory pin **v4.16.3**; host Deferred residuals not auto-promoted) (gap-fill train T0–T11; baseline **88/100** after T2/T4/T5; +1 residual WAIVE bookkeeping only).

### Score A — GrokBuild adapter scope (**100/100**)

**In contract (must ship on Grok via lfg):** core install (`setup --run`, stamp, config single-writer, agents, hook trust/normalize, skill sync); behavior-backed **Grok-adapted** / **Implemented** ports (rules, comment-checker, lsp MCP, ast_grep, codegraph, ultrawork, ulw-loop CLI + skill, ulw-plan, prompts/delegate/boulder/skills-loader cores, ultimate-browsing payload); Manifest-only / remote-URL shape rows as shape-only; Unsupported policy rows (`auto-update`, `telemetry`, `test-support`); residual **WAIVE** bookkeeping for LSP auto-reinject and missing-host/policy residuals without fake Grok-adapted flips.

**Out of contract (do not discount Score A):** planes owned by other adapters/packages or missing Grok host APIs. lfg **references** them; it does **not** claim them as Grok runtime parity.

| Out-of-contract plane | Owner / isolated package surface | Why N/A for Score A |
|----------------------|----------------------------------|---------------------|
| `omo-senpi` / `senpi` task–team RPC | `@code-yeongyu/senpi`, `omo-senpi`, `senpi-task`, `team-core` under **`~/.senpi`** | Separate control plane; see `docs/grok-orchestration-plane.md` (D)(E) |
| senpi / Codex **app-server** | senpi `app-server` / Codex app-server NDJSON | Not an lfg runtime dependency (task-11 spike: no product) |
| `codex_app` team threads | Codex host (`codex_app.create_thread`) | No Grok equivalent; Grok teammode uses `spawn_subagent` instead — `codex_app` plane **N/A for Score A** |
| Automatic Stop/SubagentStop reinject | Host lifecycle enforcement beyond guidance | Sisyphus + `lfg ulw-loop` substitutes are in-scope; **auto reinject** is out of contract |
| `workflow-selector` / `plan-mode-interception` as native intercept | Missing Grok prompt/Plan Mode intercept surface | Guidance-only substitutes in-scope; native intercept out of contract |
| Codex `bootstrap` of Codex runtime deps | Codex SessionStart provisioning | Policy: lfg does not bootstrap Codex from Grok |

**Score A formula:** all **in-contract** obligations met with evidence (install gates, behavior tests, residual WAIVEs recorded) → **100/100**. Out-of-contract rows remain listed as Deferred/Unsupported in the component table for inventory honesty; they **do not** reduce Score A.

### Score B — Full OMO host surface (**89/100**)

Same table as before: Deferred host-bound rows **discount** this score even when Score A treats them as out of contract.

#### What Score B counts

- **Full weight:** shipped core install parity; behavior-backed **Grok-adapted** / **Implemented** component ports (runtime/hook/tool proof, not file copy alone).
- **Partial / shape weight:** installed **Manifest-only** and **Remote URL manifest-only** surfaces (shape-validated; not behavioral ports).
- **Closed bookkeeping:** explicitly **Unsupported** non-runtime upstream pieces (`auto-update`, `telemetry`, `test-support`) already reflected in the 88 baseline.
- **Discounted:** **Deferred** host-bound behavior (orchestration-plane **host dependency class** per `docs/grok-orchestration-plane.md`: `codex_app`, `Stop/SubagentStop hook`, `missing host surface`, `policy / no Codex bootstrap from Grok`). Deferred MVP substitutes (ledger, pure helpers, guidance hooks) **do not** auto-promote status or invent large score jumps.

#### Honest formula (gap-fill train → Score B)

| Layer | Δ | Result |
|---|---:|---:|
| Baseline after T2/T4/T5 (comment-checker, ast_grep, lsp MCP Grok-adapted + core install) | — | **88** |
| Status promotions Deferred → Grok-adapted/Implemented | **+0** | 88 (gap-fill train; **teammode** later Grok-adapted via spawn_subagent dual catalogs, score bookkeeping unchanged) |
| Explicit residual **WAIVE** bookkeeping (T8 LSP reinject residual path + T9 pack for workflow-selector / plan-mode-interception / bootstrap) | **+1** | **89** (docs/inventory honesty only; not behavior) |
| Prove-only MVP strengthen (T3 SubagentStop verifier wiring, T5 start-work resume wording, T6 team-ledger unit e2e, T7 HOLD) | **+0** | 89 |
| **Score B total** | | **89/100** |

**Not Score B 90–93:** the gap-fill plan’s 90–93 band assumed promotions that **did not land**. Claiming Score B 93 without status flips would invent points. **Score A 100 does not imply Score B 100.**

**Still Deferred on the component table (Score B residual; Score A out-of-contract or WAIVE):** `workflow-selector` (lfg optional historical; upstream removed #5745), `difficulty-tier-workers`, `lazycodex-executor-verify`, `start-work-continuation`, `bootstrap`, `plan-mode-interception`, `git-bash` Windows behavior, and automatic LSP lifecycle hook reinjection. **teammode Grok-adapted** (spawn_subagent + dual agent catalogs); former **T7 HOLD Deferred** residual is mailbox-only (issue [#100](https://github.com/islee23520/lfg/issues/100); `T7-promote-or-hold.md`). **T8 residual WAIVE** for LSP reinject (issue [#101](https://github.com/islee23520/lfg/issues/101); `T8-lsp-residual.txt`). **T9 residual WAIVE pack** (issue [#102](https://github.com/islee23520/lfg/issues/102); `T9-residuals.txt`): `workflow-selector` and `plan-mode-interception` stay Deferred (**host dependency class: missing host surface**); `bootstrap` stays Deferred (**host dependency class: policy / no Codex bootstrap from Grok**). No fake Grok-adapted claims for these three.

**Score evidence:** `.omo/evidence/omo-parity-gap-fill-88-to-95/` (`T0-baseline.md` … `T11-score.txt`); dual-score note `.omo/evidence/omo-parity-gap-fill-88-to-95/DUAL-SCORE-100-adapter-scope.md`. Follow-ups (hygiene train closed issues may still document promote residuals): [#98](https://github.com/islee23520/lfg/issues/98), [#99](https://github.com/islee23520/lfg/issues/99), [#100](https://github.com/islee23520/lfg/issues/100), [#101](https://github.com/islee23520/lfg/issues/101), [#102](https://github.com/islee23520/lfg/issues/102), [#103](https://github.com/islee23520/lfg/issues/103), [#104](https://github.com/islee23520/lfg/issues/104).

## Agent/Model Source Map

The T5 upstream agent/model declaration mapping is tracked in
`docs/grok-adapter-agent-model-source-map.md`.

## Core Install Parity

| omo-codex capability | Codex reference | Grok owner (lfg) | Status |
|----------------------|-----------------|------------------|--------|
| Plugin cache install | `~/.codex/plugins/cache/sisyphuslabs/omo/` | `src/grok/` | Implemented (`plugin-cache-install.acceptance.test.ts` #27; `setup-doctor-parity.test.ts` postInstallVerify #21) |
| `config.toml` merge | `install/config.mjs` | `grok-install` + `lfg-grok-config.ts` | Implemented (`config-single-writer.acceptance.test.ts` #29; `lfg-installer.contract.test.ts`; `lfg-grok-config.endpoints` #24; `grok-config-endpoints-doc.test.ts`; `docs/grok-config-endpoints.md`; `LFG_OWNED_GROK_CONFIG_SECTIONS`) |
| Agent TOML + preserve reasoning | `install/agents.mjs` | `grok-install/apply-agent-tomls.ts` + `sync-lazycodex-agents-to-grok.ts` | Implemented (`agent-tomls.acceptance.test.ts` #30; `apply-agent-tomls.test.ts`; `sync-lazycodex-agents.test.ts`; `no-linalab-branding.test.ts`). `setup --run` now materializes native OMO agent surfaces, including a Grok-native OMO Hephaestus default prompt and lfg-owned Sisyphus/Atlas planning and research prompts. |
| Hook trust | `install/hook-trust.mjs` | `grok-install/hook-trust.ts` | Implemented (`hook-trust.acceptance.test.ts` #28; `post-install-verify.test.ts`; internal verifier `hooksRegistered`) |
| Install version stamp | `lazycodex-install.json` | `lfg-install.json` (Grok plugin root) | Implemented (`plugin-cache-install.acceptance.test.ts` #27; `npm-publish-root-contract.test.ts` #22; `publish-gap-evidence-shape.test.ts`) |
| Internal verifier | `src/cli/doctor/checks/codex.ts` | `grok-install/doctor.ts` (not public CLI) | Implemented (`doctor-pack-layout.acceptance.test.ts` #25; `doctor-json-contract.test.ts` #31; `setup-doctor-parity.test.ts` #21; `publish-owner-checklist.test.ts`; `registry-bin-publish-gap.test.ts`; `publishGap` #22) |
| `cleanup` / `update` | passthrough CLI | `docs/grok-cleanup-update.md` | N/A (`grok-cleanup-update-doc.test.ts` #34; re-run `setup --run`) |
| OMO skill payload sync | aggregate/plugin skills + component-local skills | `scripts/sync-omo-skills-to-grok.mjs` → `src/grok/skills`, package `skills/`, `dist/grok-install/skills` | Implemented as an upstream-derived GrokBuild adapter payload (`omo-skill-sync-script.test.ts`; `run-grok-install.test.ts`; package tests): the sync step imports OMO aggregate skills, including `ultimate-browsing` with its references/engine/scripts, plus component-local sources for `comment-checker`, `lsp`, `rules`, `ulw-loop`, and `ulw-plan`, preserves relative references/scripts, converts OpenAI agent metadata to `agents/grok.yaml`, converts LazyCodex maintenance skills to lfg-owned `lfg-doctor`, `lfg-report-bug`, and `lfg-contribute-bug-fix`, and writes `.lfg-omo-skill-sync.json` provenance. This is skill availability, not a claim that every deferred component runtime or stealth-browser surface is behavior-adapted. |
| ulw-loop / start-work skills | plugin components + project `.omo` ledger | Grok plugin tree + `grok-install/assets/lfg-config-loader.mjs` + upstream OMO skill directories | Implemented for lfg-owned hook-time project `.omo` awareness (`project-omo-ledger.test.ts`; `lfg-config-loader.test.ts`; `omo-loader-runtime.integration.test.ts`) and upstream-derived skill payload installation (`run-grok-install.test.ts`; `post-install-verify.ts`): concise active work/status/plan/worktree plus ledger existence and line count only; no ledger tail/content; malformed `.omo` fails closed. `start-work-continuation` lifecycle CLI remains Deferred below. |
| Project-local `.grok` repair | `project-local-cleanup.mjs` | `grok-install/project-local.ts` | N/A (internal inspect helper; public CLI remains setup-only; automated repair deferred — `repair` field in JSON; `lfg-project-local.test.ts` #28) |
| Model catalog | `model-catalog.json` | `lfg-models.ts` + `LAZYCODEX_*` | Implemented (`lfg-models.mapping.test.ts`; `lfg-models.urls.test.ts`; setup discovery env) |
| Autonomous permissions | `permissions.mjs` | N/A or Grok permissions | N/A |
| Telemetry | plugin telemetry | vendored in tree | N/A (upstream plugin telemetry; lfg does not emit) |
| Extension hooks (LFP port) | legacy LFP | `grok-install/extension-hooks.ts` | Implemented (`post-install-ported-hooks.test.ts` #32; `extension-hooks.test.ts`; `extension-hooks.catalog.test.ts`; `hook-trust`). **Grok setup installs native first-party OMO hooks**; bridge fallback used only for legacy/imported hooks. |
| Extension agent overrides (LFP port) | legacy LFP | `grok-install/agent-overrides.ts` | Implemented (`agent-overrides.test.ts` #30; `apply-agent-tomls.ts` merge) |
| Per-agent model overrides (LFP-style) | LFP `omo-agent-model-overrides` + `agent-config` | `lazycodex-agent-overrides.ts` + `sync-lazycodex-agents-to-grok.ts` | Implemented (`lazycodex-agent-overrides.test.ts`; `~/.grok/lazycodex-agent-overrides.json`; interactive setup) |

## Full OMO Component Parity

Current upstream baseline: `lazycodex-ai` / OMO `v4.16.3`
(https://github.com/code-yeongyu/oh-my-openagent/releases/tag/v4.16.3), verified
on 2026-06-26. `lfg-component-inventory.json` records this baseline in each
fresh Grok setup so installed support can be audited against the tracked OMO
release while keeping lfg focused on the GrokBuild port and installed Grok plugin payload.

Upstream component inventory is from
`lazycodex-ai@4.16.3` / `oh-my-openagent` tag `v4.16.3`
(`21561c205e05d394c8734a660624b31d39bd0843`). Each row cites the upstream source
plus the local owner/test surface. The status vocabulary for this table is:
`Implemented`, `Grok-adapted`, `Manifest-only`, `Remote URL manifest-only`, `Unsupported`, or `Deferred`.

Automatic upkeep is enforced by `scripts/omo-parity-upkeep.mjs`, which scans a configured OMO/LazyCodex source tree for aggregate skills, shared-skills package entries, component-local skills, plugin components, split hook JSON component references, and package inventory. `npm run assert-omo-parity` invokes that scanner in local generated-payload mode on every parity gate, and the scanner can be pointed at a fresh upstream checkout with `npm run omo-parity-upkeep -- --source <oh-my-openagent-or-lazycodex-ai-root>` to fail on any unclassified upstream skill, component, or hook-referenced component before `lfg-component-inventory.json` and this table are updated.

The `v4.16.3` baseline tracks; the earlier `v4.13.0` refresh added upstream package-layout skills, `teammode`,
`lazycodex-executor-verify`, `workflow-selector`, `test-support`, split hook JSON files under
`packages/omo-codex/plugin/hooks/`, and package-level MCP runtimes for
`git-bash-mcp` and `lsp-daemon`. lfg now syncs package-layout skills and can
copy available package MCP runtimes while packaging lfg-owned behavior for
`comment-checker`, `ast_grep`, and `lsp`. Unported Codex/OpenCode-specific hooks remain Deferred until
there is Grok-native host glue; manifest-only MCP entries are not behavior-adapted local MCP tools.
Upstream package test infrastructure remains Unsupported because it is test-only support code outside the Grok runtime payload.

| upstream component | upstream source | local owner / tests | Grok/lfg support | Status |
|--------------------|-----------------|---------------------|------------------|--------|
| `comment-checker` | `packages/omo-codex/MARKETPLACE.md`; `components/comment-checker` | `src/grok/payload/component-inventory.ts`; `comment-checker-hook.test.ts`; `T2-comment-checker-runtime.txt`; `plugin-cache-install.acceptance.test.ts` | Native Grok PostToolUse hook is installed as `hooks/lfg-native-comment-checker.mjs`. T2 proof shows bounded feedback for noisy comments, silence for clean files, and fail-closed malformed JSON handling. | Grok-adapted |
| `git-bash` | `packages/omo-codex/plugin/components/git-bash`; `packages/git-bash-mcp` | `src/grok/payload/component-inventory.ts`; `plugin-cache-install.acceptance.test.ts`; `materialize-grok-mcp.test.ts` | Manifest-only for Grok parity. lfg can copy the upstream 4.13.0 `git-bash-mcp` runtime from a package-shaped source, but macOS/non-Windows installs keep it disabled through `disabled_mcp_servers`; Windows behavior remains Windows-unverified. | Manifest-only |
| `rules` | `packages/omo-codex/MARKETPLACE.md`; `components/rules` | `src/core/omo/rules-engine` (host-neutral core); `src/grok/ports/rules-injector.ts` (Grok PostToolUse glue); `hook-bridge.integration.test.ts`; `post-install-ported-hooks.test.ts` | **Native first-party OMO hooks** installed by Grok `lfg setup --run` (https://github.com/code-yeongyu/oh-my-openagent codex adapter + opencode). Bridge fallback (`lfg-grok-hook-bridge.mjs`) only for legacy/imported hooks. Phase 1 of the core/adapter port strategy: `rules-engine` is owned under `src/core/omo` and wired into Grok via `rules-injector.ts` (PostToolUse → discover+match rules/AGENTS.md → context block). | Grok-adapted |
| `lsp` | `packages/omo-codex/plugin/components/lsp`; `packages/lsp-daemon`; `packages/lsp-tools-mcp` | `src/grok/payload/component-inventory.ts`; `grok-adapter-parity-doc.test.ts`; `lsp-mcp-runtime.test.ts`; `materialize-grok-mcp.test.ts`; `T5-lsp-runtime.txt`; `T8-lsp-residual.txt` | Bundled `lfg-lsp` MCP runtime exposes `typescript_diagnostics`, returns typed malformed-input errors, reports TS2322 for dirty TypeScript fixtures, and returns empty diagnostics for clean fixtures. **T8 residual WAIVE:** MCP runtime Grok-adapted; automatic PostToolUse/PostCompact reinjection not claimed (not a cheap comment-checker-class pure-file hook; language-service lifecycle remains unclaimed). | Grok-adapted |
| `ast_grep` | upstream aggregate + build-bundled-mcp-runtimes | `src/grok/payload/component-inventory.ts`; MCP manifest + doctor; `materialize-grok-mcp.test.ts`; `T4-ast-grep-runtime.txt` | Bundled `lfg-ast_grep` MCP runtime exposes `ast_grep_search`, returns structural matches, reports typed `malformed_pattern` and `invalid_path` errors, and uses a deterministic fallback when `sg` is unavailable. | Grok-adapted |
| `codegraph` | `packages/utils/src/codegraph/`; `omo-codex/plugin/components/codegraph`; `omo-opencode/mcp/codegraph.ts` | `src/grok/payload/component-inventory.ts`; `docs/grok-adapter-core-port-strategy.md` (Phase 0) | External `@colbymchenry/codegraph` semantic-code-graph MCP binary wrapped via host-neutral `utils/codegraph` provisioning (sha256-verified download to `~/.omo/codegraph`) + Grok-native `.mcp.json` command server (`serve --mcp`). Self-contained, parallel-safe; graph intelligence lives in the external binary, not OMO. | Grok-adapted |
| `grep_app` | upstream aggregate + remote MCP manifest | `src/grok/payload/component-inventory.ts`; MCP manifest + doctor | grep_app MCP is represented as the upstream remote URL server `https://mcp.grep.app`; lfg validates manifest shape and does not live-call it by default. | Remote URL manifest-only |
| `context7` | upstream aggregate + remote MCP manifest | `src/grok/payload/component-inventory.ts`; MCP manifest + doctor | context7 MCP is represented as the upstream remote URL server `https://mcp.context7.com/mcp`; lfg validates manifest shape and does not live-call it by default. | Remote URL manifest-only |
| `ultrawork` | `packages/omo-codex/MARKETPLACE.md`; `components/ultrawork` | `src/grok/agents/sync-lazycodex-agents-to-grok.ts`; `sync-lazycodex-agents.test.ts` | **Native first-party OMO hooks** (Grok-first OMO parity via https://github.com/code-yeongyu/oh-my-openagent codex adapter + opencode feature). Ultrawork bridged only for legacy; agent prompts synced. | Grok-adapted |
| `ulw-loop` | `packages/omo-codex/MARKETPLACE.md`; `components/ulw-loop`; upstream OMO `skills/ulw-loop/` | `src/grok/assets/lfg-config-loader.mjs`; `src/grok/skills/ulw-loop/`; `project-omo-ledger.test.ts`; `omo-loader-runtime.integration.test.ts`; `run-grok-install.test.ts` | Project `.omo` awareness is installed fail-closed through `lfg-config-loader.mjs`; `ulw-loop` session count + active ledger presence is reported in hook context when `.omo/ulw-loop/<session>/ledger.jsonl` exists. The package installs the upstream OMO `ulw-loop` skill directory, including `references/full-workflow.md`, and packages a durable CLI as `lfg ulw-loop` / `lfg ulw` under `src/core/omo/ulw-loop`. | Grok-adapted |
| `ulw-plan` | upstream OMO `skills/ulw-plan/` | `src/grok/skills/ulw-plan/`; `run-grok-install.test.ts`; `post-install-verify.ts` | The package installs the upstream OMO `ulw-plan` skill directory, including `references/full-workflow.md`, intent references, and `scripts/scaffold-plan.mjs`; lfg does not replace it with a custom condensed workflow. | Grok-adapted |
| `ultimate-browsing` | upstream OMO `skills/ultimate-browsing/` | `scripts/sync-omo-skills-to-grok.mjs`; `src/grok/skills/ultimate-browsing/`; package `skills/ultimate-browsing/`; package/pack tests | The package installs the upstream OMO `ultimate-browsing` skill payload, including `SKILL.md`, references, engine, scripts, attribution, and converted `agents/grok.yaml`. This is skill payload availability; lfg does not claim a separate Grok-native stealth-browser runtime beyond the installed skill assets. | Implemented |
| `start-work-continuation` | `packages/omo-codex/MARKETPLACE.md`; `components/start-work-continuation` | `src/grok/payload/component-inventory.ts`; `grok-adapter-parity-doc.test.ts`; `src/core/omo/boulder-state` (getStopHookContinuationContext + tests); `lfg-sisyphus-hooks-evidence.test.ts` | Boulder/start-work continuation uses ledger-backed pure helper `getStopHookContinuationContext` (reads boulder.json + resolves plan path from boulder-state; emits structured context with ledgerPath suitable for Stop hook additionalContext; malformed .omo fails closed; present path names durable resume via `lfg ulw-loop` / `lfg ulw` and explicitly denies automatic reinjection; Grok-native lifecycle); the durable continuation CLI is not packaged as an automatic reinjection surface; Sisyphus native Stop/SubagentStop hooks provide context/guidance only (no auto reinjection claimed; **host dependency class: Stop/SubagentStop hook** per `docs/grok-orchestration-plane.md`; see task-6 evidence, T5-runtime.txt, and updated sisyphus-hooks.mjs). | Deferred |
| `plan-mode-interception` | Grok runtime Plan Mode / `enter_plan_mode` expectation | `src/grok/payload/component-inventory.ts`; `src/grok/assets/lfg-sisyphus-hooks.mjs`; `extension-hooks.test.ts`; `grok-adapter-parity-doc.test.ts`; `T9-residuals.txt` | **T9 residual WAIVE** (issue #102): Grok native Plan Mode interception is not implemented because no verified Grok hook/runtime surface is exposed for intercepting `enter_plan_mode` (**host dependency class: missing host surface** per `docs/grok-orchestration-plane.md`). Current `/ulw-plan` behavior is hook-time `additionalContext` guidance only, not native Plan Mode routing. Do not claim Grok-adapted/Implemented without a real intercept surface. | Deferred |
| `prompts-core` | `packages/prompts-core/src/` (upstream `oh-my-openagent`) | `src/core/omo/prompts-core`; `src/grok/ports/grok-prompt-adapter.ts`; `prompts-core-vendored.test.ts` | Phase 3 of the core/adapter port strategy: prompts-core source (types, loader, variant-resolver, prompt tables for atlas/prometheus/ultrawork/mode) is owned under `src/core/omo`; Grok glue resolves the default variant for Grok models via the fallback chain (docs/grok-adapter-core-port-strategy.md). | Grok-adapted |
| `agent-builder` | `omo-opencode/src/agents/*` + agent-builder foundation (upstream `oh-my-openagent`) | `src/core/omo/agent-builder`; `src/grok/ports/grok-agent-builder-adapter.ts`; `component-inventory.ts` | Phase 4 of the core/adapter port strategy: agent-builder foundation, dynamic-agent prompt builders, and curated builtin agent registry are owned under `src/core/omo`; Grok glue assembles Grok agent roles. Partial port: 5/9 builtin agents fully ported; oracle/metis/sisyphus/hephaestus remain deferred as host-bound. | Grok-adapted |
| `delegate-core` | `packages/delegate-core/src/` (upstream `oh-my-openagent`) | `src/core/omo/delegate-core`; `src/grok/ports/grok-delegate-adapter.ts`; `component-inventory.ts` | Phase 5 of the core/adapter port strategy: delegate-core source (model-selection, retry-patterns, retry-guidance) is owned under `src/core/omo`; Grok glue maps delegate-task model selection to Grok subagent routing (docs/grok-adapter-core-port-strategy.md). | Grok-adapted |
| `boulder-state` | `packages/boulder-state/src/` (upstream `oh-my-openagent`) | `src/core/omo/boulder-state`; `src/grok/ports/grok-delegate-adapter.ts`; `component-inventory.ts` | Phase 5 of the core/adapter port strategy: boulder-state source (plan-checklist, types, storage) is owned under `src/core/omo`; Grok glue bridges plan-checklist to the `.omo/plans` convention (docs/grok-adapter-core-port-strategy.md). | Grok-adapted |
| `skills-loader-core` | `packages/skills-loader-core/src/` (upstream `oh-my-openagent`) | `src/core/omo/skills-loader-core`; `src/grok/ports/grok-skills-loader-adapter.ts`; `component-inventory.ts` | Phase 6 of the core/adapter port strategy: skills-loader-core host-neutral primitives (config, shared, builtin-skills loader) are owned under `src/core/omo`; Grok glue discovers skills from Grok skill roots. Curated port: OpenCode-bound async discovery/runtime/tool/hooks layers and bundled SKILL.md content remain deferred. | Grok-adapted |
| `teammode` | `packages/omo-codex/plugin/components/teammode` + Grok `spawn_subagent` | `src/grok/payload/component-inventory.ts`; `skills/teammode` (`spawn_subagent` transport, `team-agents.mjs`); `src/core/lfg/team-ledger.ts`; `teammode-spawn-subagent.test.ts`; `team-ledger.test.ts`; `T6-tests.txt`; `T7-promote-or-hold.md` | **Grok-adapted:** default transport `spawn_subagent`; member types = GrokBuild built-ins (`general-purpose`, `explore`, `plan`) + lfg OMO agents (`hephaestus`, `explorer`, `coding`, `librarian`, …). Durable `.omo/teams` + bind-subagent. Former **T7 HOLD Deferred** residual is mailbox-only (no `codex_app.create_thread` / MultiAgentV2); leader + artifacts coordination — live multi-`spawn_subagent` host e2e is host-session QA, not a status gate. Codex transports remain for Codex hosts. Evidence: T6 team-ledger unit e2e + teammode-spawn-subagent tests. | Grok-adapted |
| `lazycodex-executor-verify` | `packages/omo-codex/plugin/components/lazycodex-executor-verify`; `packages/omo-codex/plugin/hooks/subagent-stop-verifying-lazycodex-executor-evidence.json` | `src/grok/payload/component-inventory.ts`; `src/grok/hooks/subagent-stop-evidence-verifier.ts`; `src/grok/assets/hooks/lfg-sisyphus-hooks.mjs`; `subagent-stop-verifier-e2e.test.ts`; `grok-adapter-parity-doc.test.ts`; `T3-runtime.txt` | Upstream SubagentStop evidence verifier targets the Codex `lazycodex-executor` agent and writes `.omo/evidence` receipt enforcement. Grok MVP + **T3:** pure `verifySubagentStopEvidence` in `src/grok/hooks/subagent-stop-evidence-verifier.ts` is wired into Sisyphus native SubagentStop (`lfg-sisyphus-hooks.mjs`) for coding\|hephaestus\|builder — `.omo/evidence` WARNING/VERIFIED via `additionalContext`, fail-closed malformed JSON (e2e proven; issue [#98](https://github.com/islee23520/lfg/issues/98)). **Still Deferred:** no dedicated host-enforced component CLI / block-or-continue registration (**host dependency class: Stop/SubagentStop hook** per `docs/grok-orchestration-plane.md`). Not a Grok-adapted status flip. | Deferred |
| `workflow-selector` | Historical post-`v4.13.0` contract at `e222452b874eb65d550fafb1d08d3aaf0d20418f`; **removed from omo-codex plugin components on path to v4.16.3** (#5745); no `components/workflow-selector/hooks/hooks.json` on latest `dev` | `src/grok/assets/hooks/lfg-native-workflow-selector.mjs`; `native-workflow-selector-hook-registration.ts`; `native-workflow-selector.test.ts`; `component-inventory.ts` | Upstream no longer ships the codex component. lfg retains optional native `UserPromptSubmit` hook (`OMO_CODEX_AUTO_WORKFLOW`); temp-home installed-command proof only. **Deferred** pending authenticated GrokBuild host receipt — not a current upstream component promote target; not Grok-adapted. | Deferred |
| `difficulty-tier-workers` | `packages/omo-codex/plugin/components/ultrawork/agents/lazycodex-worker-{low,medium,high}.toml` + difficulty-tier start-work/ulw-loop routing (v4.16.x / #6030 family) | `src/grok/payload/component-inventory.ts` | Upstream difficulty-tier workers and evidence receipts are Codex-side. **Deferred** on Grok: no host-enforced tier dispatch; agent TOML sync residual only. | Deferred |
| `bootstrap` | upstream SessionStart component | `src/grok/payload/component-inventory.ts`; `grok-adapter-parity-doc.test.ts`; `T9-residuals.txt` | **T9 residual WAIVE** (issue #102): Upstream bootstrap provisioning targets Codex runtime dependencies; lfg does not run provisioning hooks during Grok setup (**host dependency class: policy / no Codex bootstrap from Grok**). Policy hold — not a missing hook implement later by default; no Codex runtime-dep bootstrap from Grok. | Deferred |
| `auto-update` | upstream SessionStart component | `src/grok/payload/component-inventory.ts`; `grok-adapter-parity-doc.test.ts` | Upstream auto-update can run `npx lazycodex-ai@latest install`; lfg keeps updates user-controlled and does not enable this hook. | Unsupported |
| `test-support` | `packages/omo-codex/plugin/components/test-support` | `src/grok/payload/component-inventory.ts`; `scripts/omo-parity-upkeep.mjs`; `plugin-cache-install.acceptance.test.ts` | Upstream test-support is package test infrastructure for component smoke tests. It stays outside the installed Grok runtime payload. | Unsupported |
| `telemetry` | `packages/omo-codex/MARKETPLACE.md`; `components/telemetry` | `src/grok/payload/component-inventory.ts`; `plugin-cache-install.acceptance.test.ts` | lfg does not emit upstream anonymous telemetry. | Unsupported |

**Native first-party OMO hooks + bridge fallback only for legacy/imported hooks** per T11 of `.omo/plans/grok-native-omo-hooks.md`. Grok-first OMO parity via **codex adapter** core + **opencode** feature (https://github.com/code-yeongyu/oh-my-openagent).

## Hook Event Matrix

Upstream baseline: `lazycodex-ai@4.16.3` split hook JSON files under `packages/omo-codex/plugin/hooks/`. Local generated Grok hooks must either target an installed file under `~/.grok/plugins/lfg` or use the approved bridge command `hooks/lfg-grok-hook-bridge.mjs` with an installed child target. Telemetry stays disabled by default.

| Event | Upstream matcher | Upstream timeout | Upstream command | Local target decision | Local Grok behavior |
|---|---:|---:|---|---|---|
| `SessionStart` | none | 10s | `components/rules/dist/cli.js hook session-start` | Native `hooks/lfg-native-rules.mjs session-start` invokes the installed behavioral rules CLI. | Grok-adapted rule loading plus lfg config loader and Sisyphus context. |
| `SessionStart` | none | 5s | `components/telemetry/dist/cli.js hook session-start` | Unsupported. | Not generated; lfg does not emit upstream telemetry by default. |
| `SessionStart` | `^startup$` | 5s | `scripts/auto-update.mjs hook session-start` | Unsupported. | Not generated; updates remain user-controlled. |
| `SessionStart` | none | 30s | `components/bootstrap/dist/cli.js hook session-start` | Deferred (T9 residual WAIVE; host dependency class: policy / no Codex bootstrap from Grok). | Not generated; lfg setup does not bootstrap Codex runtime dependencies from Grok hooks. |
| `UserPromptSubmit` | none | 10s | `components/rules/dist/cli.js hook user-prompt-submit` | Native `hooks/lfg-native-rules.mjs user-prompt-submit` invokes the installed behavioral rules CLI. | Grok-adapted rule refresh plus lfg config loader and Sisyphus intent routing. |
| `UserPromptSubmit` | none | 5s | `components/ultrawork/dist/cli.js hook user-prompt-submit` | Native `hooks/lfg-native-ultrawork.mjs user-prompt-submit` invokes the installed behavioral ultrawork CLI through the bridge transport. | Grok-adapted ultrawork trigger. |
| `UserPromptSubmit` | none | 10s | `components/ulw-loop/dist/cli.js hook user-prompt-submit` | Deferred. | Durable `.omo` awareness is supplied by `lfg-config-loader.mjs`; no uninstalled `ulw-loop` component command is generated. |
| `UserPromptSubmit` | none | 5s | Historical selector contract `e222452b874eb65d550fafb1d08d3aaf0d20418f` (upstream component removed #5745) | Native `hooks/lfg-native-workflow-selector.mjs`, registered by setup and quiet unless `OMO_CODEX_AUTO_WORKFLOW` is enabled. | Deferred pending authenticated GrokBuild host receipt; not current upstream codex debt; no status promotion. |
| `PreToolUse` | `^Bash$` | 5s | `components/git-bash/dist/cli.js hook pre-tool-use` | Deferred. | Not generated on macOS Grok installs. |
| `PreToolUse` | `^create_goal$` | 5s | `components/ulw-loop/dist/cli.js hook pre-tool-use` | Deferred. | Sisyphus native hook supplies pre-tool guidance; no uninstalled `ulw-loop` command is generated. |
| `PostToolUse` | edit/apply regex | 30s | `components/comment-checker/dist/cli.js hook post-tool-use` | Native Grok hook via `hooks/lfg-native-comment-checker.mjs`. | Grok-adapted bounded comment feedback after edit/apply tools; malformed JSON fails closed. |
| `PostToolUse` | edit/apply regex | 60s | `components/lsp/dist/cli.js hook post-tool-use` | Deferred for automatic lifecycle hook reinjection (T8 WAIVE). | MCP runtime Grok-adapted; automatic PostToolUse/PostCompact reinjection not claimed. LSP MCP runtime is Grok-adapted through `typescript_diagnostics`; upstream automatic edit hook is not generated. |
| `PostToolUse` | `^apply_patch$` | 10s | `components/rules/dist/cli.js hook post-tool-use` | Native `hooks/lfg-native-rules.mjs post-tool-use` invokes the installed behavioral rules CLI. | Grok-adapted project-rule matching after patch edits. |
| `PostToolUse` | `^(create_thread\|codex_app\\.create_thread)$` | 10s | `components/teammode/dist/cli.js hook post-tool-use` | Deferred. | Not generated until Codex thread-title hygiene is mapped to a Grok thread/session surface. |
| `PostToolUse` | `^(codegraph[._].*\|mcp__codegraph__.*)$` | 5s | `components/codegraph/dist/cli.js hook post-tool-use` | Deferred. | Codegraph MCP itself is Grok-adapted; upstream init-guidance hook still needs Grok-native tool-name/event mapping. |
| `PostCompact` | `manual|auto` | 5s | `components/git-bash/dist/cli.js hook post-compact` | Deferred. | Not generated on macOS Grok installs. |
| `PostCompact` | `manual|auto` | 10s | `components/rules/dist/cli.js hook post-compact` | Native `hooks/lfg-native-rules.mjs post-compact` invokes the installed behavioral rules CLI. | Grok-adapted project-rule cache reset. |
| `PostCompact` | `manual|auto` | 5s | `components/lsp/dist/cli.js hook post-compact` | Deferred for automatic lifecycle hook reinjection (T8 WAIVE). | MCP runtime Grok-adapted; automatic PostToolUse/PostCompact reinjection not claimed. Upstream automatic compact hook is not generated. |
| `Stop` | none | 10s | `components/start-work-continuation/dist/cli.js hook stop` | Deferred. | Sisyphus native Stop hook supplies final-review context (durable `lfg ulw-loop` resume pointer + explicit no automatic reinjection) and boulder-state `getStopHookContinuationContext` (ledgerPath + poll/resume guidance); automatic reinjection CLI is not packaged. |
| `SubagentStop` | none | 10s | `components/start-work-continuation/dist/cli.js hook subagent-stop` | Deferred. | Sisyphus native SubagentStop hook supplies delegation-result context (ledger-backed; names `lfg ulw-loop`; no auto reinjection); automatic reinjection CLI is not packaged. |
| `SubagentStop` | `^lazycodex-executor$` | 10s | `components/lazycodex-executor-verify/dist/cli.js hook subagent-stop` | Deferred (MVP + T3 Sisyphus wire). | Pure `verifySubagentStopEvidence` wired into Sisyphus SubagentStop for coding/hephaestus/builder (`.omo/evidence` WARNING/VERIFIED + fail-closed; T3-runtime.txt; issue #98). No dedicated host-enforced verifier CLI / block-or-continue; remains Deferred (not Grok-adapted). |

**Normative port map:** `docs/lfp-capability-port.md`
**Ownership:** `docs/grok-adapter-ownership.md` (ADR; tested in `grok-adapter-ownership-doc.test.ts`)
