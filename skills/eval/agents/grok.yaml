interface:
  display_name: "eval (code mode)"
  short_description: "Persistent JS/Python kernels for incremental code execution"
  default_prompt: "Use the eval MCP tools for multi-step code; one cell per call; reuse state."
