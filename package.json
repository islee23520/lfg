{
  "name": "lfg",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "plugins/lfg"
  ],
  "scripts": {
    "test": "bun test plugins/lfg/bin",
    "typecheck": "tsc --noEmit",
    "build": "bun run --filter './plugins/lfg' build"
  },
  "devDependencies": {
    "bun-types": "1.3.14"
  }
}
